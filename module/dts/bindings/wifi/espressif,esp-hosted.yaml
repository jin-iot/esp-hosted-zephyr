description: esp-hosted WiFi module

compatible: "espressif,esp-hosted"

properties:
  reset-gpios:
    type: phandle-array
    required: true

  data-ready-gpios:
    type: phandle-array
    required: true

  handshake-gpios:
    type: phandle-array

include: spi-device.yaml